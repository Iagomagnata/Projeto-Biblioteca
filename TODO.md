# TODO - Remoção de usuários de teste e delete de livros (Admin)

- [ ] Adicionar rota `DELETE /api/livros/:id` no `js/Server.js`
- [ ] Criar função no `js/Script.js` para chamar delete de livro e recarregar lista/atualizar UI
- [ ] Implementar UI na `pagAdm.html` para listar livros e ter botão “Excluir” para cada livro
- [x] Garantir que apenas `usuarios` seja limpo no banco (sem mexer em `livros`) (pendente de execução; travado por falta de sqlite3 no ambiente/CLI)

- [x] Ajuste de delete: botão voltar a funcionar (delete via API reabilitado)




